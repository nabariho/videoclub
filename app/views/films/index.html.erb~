<h1>Listing films</h1>

<table class="tabla" style="width: 100%">
  <tr>
    <th>Title</th>
    <th>Year</th>
    <th>Time</th>
    <th>Rental price</th>
    <th>Buy price</th>
    <th>Sinopsis</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @films.each do |film| %>
  <tr>
    <td><%= film.title %></td>
    <td><%= film.year %></td>
    <td><%= film.time %></td>
    <td><%= film.rental_price %></td>
    <td><%= film.buy_price %></td>
    <td><%= film.sinopsis %></td>
    <td class="btn-tabla"><%= link_to 'Rents', film_rents_path(film.id), :class => "btn btn-mini btn-blue" %></td>
    <td class="btn-tabla"><%= link_to 'Show', film, :class => "btn btn-mini btn-blue" %></td>
    <td class="btn-tabla"><%= link_to 'Edit', edit_film_path(film), :class => "btn btn-mini btn-blue" %></td>
    <td class="btn-tabla"><%= link_to 'Destroy', film, confirm: 'Are you sure?', method: :delete, :class => "btn btn-mini btn-blue" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Film', new_film_path, :class => "btn btn-mini btn-blue" %>
