<h1>Mis tarjetas de crédito</h1>

<table class="tabla" style="width: 100%">
  <tr>
    <th>Nº de tarjeta</th>
    <th>Fecha de caducidad</th>
    <th>Codigo de seguridad</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user.cards.each do |card| %>
  <tr>
    <td><%= card.n_cuenta %></td>
    <td><%= card.fecha %></td>
    <td><%= card.secure_code %></td>
    <td class="btn-tabla"><%= link_to 'Show', card, :class => "btn btn-mini btn-blue"%></td>
    <td class="btn-tabla"><%= link_to 'Edit', edit_card_path(card), :class => "btn btn-mini btn-blue" %></td>
    <td class="btn-tabla"><%= link_to 'Destroy', card, confirm: 'Are you sure?', method: :delete, :class => "btn btn-mini btn-blue" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Card', new_card_path, :class => "btn btn-mini btn-blue" %>
