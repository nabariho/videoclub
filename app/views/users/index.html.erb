<h1>Gestión de Usuarios</h1>

<table class="tabla">
  <tr>
    <th>Nombre de Usuario</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td class="btn-tabla"><%= link_to 'Alquileres/Compras', user_rents_path(user.id), :class => 'btn btn-mini btn-blue' %></td>
    <td class="btn-tabla"><%= link_to 'Comentarios', user_comments_path(user.id), :class => 'btn btn-mini btn-blue' %></td>
    <td class="btn-tabla"><%= link_to 'Eliminar', user, confirm: 'Are you sure?', method: :delete, :class => 'btn btn-mini btn-blue' %></td>
  </tr>
<% end %>
</table>

<br />
